// explicit types
var character;
var age;
var isLoggedIn;
age = 20;
// arrays
var ninjas = []; // an array that only accepts strings, thats assigned to an empy array
// union type..our way of saying it can be 1 of 2, types
var mixed = []; // an array that can accept array OR strings, thats assigned to an empty array
mixed.push('duane');
console.log(mixed);
// objects
var ninjaOne;
